import 'package:flutter/material.dart';

class Boosts extends StatelessWidget {
  const Boosts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
      body: Center(child: Text("boosts")),
    );
  }
}
